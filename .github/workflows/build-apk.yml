name: 📱 Build Novado APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-apk:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      
    - name: 📦 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: 📋 Install dependencies
      run: npm install --legacy-peer-deps
      
    - name: 🏗️ Build PWA
      run: npm run build
      
    - name: ☕ Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
        
    - name: 🤖 Setup Android SDK
      uses: android-actions/setup-android@v3
      
    - name: 🔧 Setup Gradle 8.2
      uses: gradle/gradle-build-action@v2
      with:
        gradle-version: '8.2'
        
    - name: 📱 Accept Android licenses
      run: yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses
      
    - name: 📱 Install Android build tools
      run: |
        $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "build-tools;34.0.0"
        $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "platforms;android-34"
        
    - name: ⚙️ Create Android project
      run: |
        npx cap add android
        npx cap sync android
        
    - name: 🔧 Update Gradle wrapper to 8.2
      run: |
        cd android
        
        # Update gradle-wrapper.properties to use Gradle 8.2
        echo "distributionBase=GRADLE_USER_HOME" > gradle/wrapper/gradle-wrapper.properties
        echo "distributionPath=wrapper/dists" >> gradle/wrapper/gradle-wrapper.properties
        echo "distributionUrl=https\\://services.gradle.org/distributions/gradle-8.2-all.zip" >> gradle/wrapper/gradle-wrapper.properties
        echo "zipStoreBase=GRADLE_USER_HOME" >> gradle/wrapper/gradle-wrapper.properties
        echo "zipStorePath=wrapper/dists" >> gradle/wrapper/gradle-wrapper.properties
        
        chmod +x gradlew
        
        echo "✅ Gradle wrapper updated to 8.2"
        
    - name: 🏗️ Build APK
      run: |
        cd android
        ./gradlew assembleDebug
        
    - name: 📋 Get version
      id: version
      run: echo "VERSION=$(date +'%Y.%m.%d.%H%M')" >> $GITHUB_OUTPUT
      
    - name: 📦 Prepare APK for download
      run: |
        # Copy APK to easy access location
        cp android/app/build/outputs/apk/debug/app-debug.apk novado-v${{ steps.version.outputs.VERSION }}.apk
        
        echo "✅ APK готовий до завантаження:"
        ls -la novado-v${{ steps.version.outputs.VERSION }}.apk
        
        echo "📱 Розмір APK:"
        du -sh novado-v${{ steps.version.outputs.VERSION }}.apk
        
    - name: 📤 Upload APK as artifact
      uses: actions/upload-artifact@v4
      with:
        name: novado-apk-v${{ steps.version.outputs.VERSION }}
        path: novado-v${{ steps.version.outputs.VERSION }}.apk
        retention-days: 30
        
    - name: 📋 Build success summary
      run: |
        echo "🎉 NOVADO APK ЗБУДОВАНО УСПІШНО!"
        echo "================================"
        echo "📅 Версія: v${{ steps.version.outputs.VERSION }}"
        echo "📱 Назва: Novado"
        echo "🇺🇦 Слоган: Безліч оголошень чекають на тебе"
        echo ""
        echo "📥 ЯК ЗАВАНТАЖИТИ APK:"
        echo "1. GitHub → Actions → Цей workflow"
        echo "2. Прокрутіть вниз до 'Artifacts'"
        echo "3. Клікніть 'novado-apk-v${{ steps.version.outputs.VERSION }}'"
        echo "4. Завантажте ZIP та розпакуйте APK"
        echo ""
        echo "📱 ЯК ВСТАНОВИТИ:"
        echo "1. Увімкніть 'Невідомі джерела' в Android"
        echo "2. Встановіть APK файл"
        echo "3. Відкрийте додаток Novado"
        echo ""
        echo "✨ ФУНКЦІЇ ДОДАТКУ:"
        echo "- 🎬 Splash screen з анімацією"
        echo "- 📱 Іконка з буквою N"
        echo "- 🔄 Автооновлення"
        echo "- 🇺🇦 Український брендинг"