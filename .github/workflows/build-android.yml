name: 📱 Android APK (Ultra Simple)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout
      uses: actions/checkout@v4
      
    - name: 📦 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: 📋 Install dependencies
      run: npm install --legacy-peer-deps
      
    - name: 🏗️ Build PWA
      run: npm run build
      
    - name: ☕ Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
        
    - name: 🤖 Setup Android SDK
      uses: android-actions/setup-android@v3
      
    - name: 📱 Accept SDK licenses
      run: yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses || true
      
    - name: 🔧 Install Gradle
      run: |
        sudo apt-get update
        sudo apt-get install -y gradle
        gradle --version
        
    - name: ⚙️ Setup Capacitor
      run: |
        # Полная очистка и пересоздание
        rm -rf android/ node_modules/.cache/
        
        # Инициализация
        npx cap init "Novado" "com.novado.app" --web-dir=dist
        npx cap add android
        npx cap sync android
        
    - name: 🔧 Fix Android project
      run: |
        cd android
        
        # Создаем правильный gradlew wrapper
        gradle wrapper --gradle-version 8.0.2 --distribution-type bin
        
        # Проверяем что создалось
        echo "📁 Gradle wrapper files:"
        ls -la gradlew* gradle/wrapper/ 2>/dev/null || echo "Some files missing"
        
        # Убеждаемся что gradlew исполняемый
        chmod +x gradlew
        
        # Тестируем gradlew
        ./gradlew --version || echo "gradlew test failed"
        
    - name: 🏗️ Build APK
      run: |
        cd android
        
        echo "🚀 Building APK..."
        ./gradlew assembleDebug --stacktrace --info
        
        # Проверяем результат
        if [ -f "app/build/outputs/apk/debug/app-debug.apk" ]; then
          echo "✅ APK built successfully!"
          ls -la app/build/outputs/apk/debug/
          du -sh app/build/outputs/apk/debug/app-debug.apk
        else
          echo "❌ APK not found!"
          find . -name "*.apk" -type f 2>/dev/null || echo "No APK files found"
          exit 1
        fi
        
    - name: 📋 Get version
      id: version
      run: echo "VERSION=$(date +'%Y.%m.%d-%H%M')" >> $GITHUB_OUTPUT
      
    - name: 🚀 Create Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v${{ steps.version.outputs.VERSION }}
        name: 📱 Novado v${{ steps.version.outputs.VERSION }}
        body: |
          📱 **Novado Android APK готовий!**
          
          📅 Версія: ${{ steps.version.outputs.VERSION }}
          📱 Платформа: Android
          🏷️ Назва програми: **Novado**
          
          ### 📥 Встановлення:
          1. Завантажте APK нижче
          2. Увімкніть "Невідомі джерела" в налаштуваннях Android
          3. Встановіть APK файл
          4. Відкрийте додаток **Novado**
          
          ### ✨ Функції додатку:
          - 🎬 Красивий splash screen з анімацією
          - 📱 Іконка з буквою N у градієнті
          - 🇺🇦 Слоган: "Безліч оголошень чекають на тебе"
          - 🔄 Автооновлення через PWA технології
          - 🌐 Повна функціональність веб-версії
          - 📱 Нативне поведінка Android додатку
          
          ### 🔄 Автооновлення:
          - Додаток автоматично перевіряє оновлення
          - Нові функції з'являються без переустановки
          - Синхронізація з веб-версією
          
        files: android/app/build/outputs/apk/debug/app-debug.apk
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}