name: 📱 Build Android APK

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-web:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      
    - name: 📦 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: 📋 Install dependencies
      run: npm ci
      
    - name: 🏗️ Build web app
      run: npm run build
      
    - name: 📤 Upload web build
      uses: actions/upload-artifact@v4
      with:
        name: web-build
        path: dist/

  build-android:
    needs: build-web
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Checkout code
      uses: actions/checkout@v4
      
    - name: 📦 Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'
        cache: 'npm'
        
    - name: 📥 Download web build
      uses: actions/download-artifact@v4
      with:
        name: web-build
        path: dist/
        
    - name: ☕ Setup Java
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'
        
    - name: 🤖 Setup Android SDK
      uses: android-actions/setup-android@v3
      
    - name: 📱 Install Capacitor
      run: |
        npm install -g @capacitor/cli
        npm install @capacitor/core @capacitor/android
        
    - name: ⚙️ Initialize Capacitor
      run: |
        npx cap init "Novado" "com.novado.app" --web-dir=dist
        
    - name: 🤖 Add Android platform
      run: npx cap add android
      
    - name: 🔄 Sync with Android
      run: npx cap sync android
      
    - name: 🏗️ Build Android APK
      run: |
        cd android
        ./gradlew assembleDebug
        
    - name: 📤 Upload APK
      uses: actions/upload-artifact@v4
      with:
        name: android-apk
        path: android/app/build/outputs/apk/debug/app-debug.apk

  release:
    needs: build-android
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
    - name: 📥 Download APK
      uses: actions/download-artifact@v4
      with:
        name: android-apk
        
    - name: 📋 Get version
      id: version
      run: echo "VERSION=$(date +'%Y.%m.%d.%H%M')" >> $GITHUB_OUTPUT
      
    - name: 🚀 Create Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: v${{ steps.version.outputs.VERSION }}
        name: Novado v${{ steps.version.outputs.VERSION }}
        body: |
          🚀 **Автоматична збірка APK**
          
          📅 **Дата**: ${{ steps.version.outputs.VERSION }}
          📱 **Платформа**: Android
          🔄 **Тип**: Debug Build
          
          ### 📥 Завантаження:
          - Завантажте `novado-v${{ steps.version.outputs.VERSION }}.apk`
          - Увімкніть "Невідомі джерела" в налаштуваннях Android
          - Встановіть APK файл
          
          ### 🔄 Автооновлення:
          - Приложення автоматично перевірятиме оновлення
          - PWA функції забезпечують синхронізацію з веб-версією
          
        files: |
          app-debug.apk
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}